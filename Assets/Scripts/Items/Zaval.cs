using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Zaval : MonoBehaviour
{
    // Start is called before the first frame update
    public void Clean() {
        Destroy(this.gameObject);
    }
}
